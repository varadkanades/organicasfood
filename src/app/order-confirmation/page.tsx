// TODO: Phase 4 — Order confirmation with details + WhatsApp opt-in
import Container from "@/components/ui/Container";

export const metadata = {
  title: "Order Confirmed — Organika's Food",
};

export default function OrderConfirmationPage() {
  return (
    <Container className="py-12 text-center">
      <p className="text-mid-gray">[Order Confirmation — Phase 4]</p>
    </Container>
  );
}
