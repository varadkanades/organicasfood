import LoginPage from "./LoginPage";

export const metadata = {
  title: "Sign In",
  description: "Sign in or create an account at Organika's Food.",
};

export default function Page() {
  return <LoginPage />;
}